import styled from "styled-components";

export const StyledButtonContainer = styled.div`
  position: fixed;
  bottom: 3rem;
  left: 0;
  width: 100%;
  display: flex;

  & > button {
    margin: auto;
  }
`;
